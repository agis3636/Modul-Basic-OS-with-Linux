Wah, mantap! Filenya sudah masuk dan sudah kamu ubah izinnya (meskipun sebenarnya `777` itu agak berlebihan, tapi untuk instalasi tidak masalah). File yang kamu punya adalah versi **Network Edition 10.1.0** untuk **Ubuntu 22.04**.

Berikut adalah langkah-langkah instalasinya secara berurutan:

---

### 1. Ekstrak File Installer

Karena file berakhir dengan `.tgz`, kamu harus mengekstraknya terlebih dahulu:

```bash
tar -xzvf zcs-NETWORK-10.1.0_GA_4655.UBUNTU22_64.20240819064312.tgz

```

### 2. Masuk ke Folder Hasil Ekstrak

Gunakan perintah `cd` untuk masuk ke folder yang baru muncul:

```bash
cd zcs-NETWORK-10.1.0_GA_4655.UBUNTU22_64.20240819064312

```
